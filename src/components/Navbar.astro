---
import {
  ArrowRight,
  Banknote,
  BarChart3,
  Bot,
  Briefcase,
  BrainCircuit,
  ChevronDown,
  Code2,
  Database,
  Factory,
  FileText,
  GraduationCap,
  HeartPulse,
  Headphones,
  KeyRound,
  Landmark,
  Menu,
  Megaphone,
  Network,
  Puzzle,
  Search,
  Settings,
  Server,
  ShieldCheck,
  ShoppingBag,
  Sparkles,
  Truck,
  User,
  Users,
  X,
  Zap,
} from "@lucide/astro";
import { getBrandEmail } from "../lib/site";

interface NavColumn {
  title: string;
  links: Array<{ label: string; href: string; description?: string; icon?: typeof Zap; nested?: boolean }>;
}

interface NavGroup {
  label: string;
  href: string;
  featured?: {
    title: string;
    href: string;
    description: string;
    icon: typeof Zap;
  };
  columns: NavColumn[];
}

interface Props {
  currentPath?: string;
  ctaLabel?: string;
  ctaHref?: string;
  theme?: "dark" | "light";
}

const { currentPath = "/", ctaLabel = "Book demo", ctaHref = "/contact", theme = "light" } = Astro.props;
const brandEmail = getBrandEmail();
const CONTAINER = "mx-auto w-full max-w-[1240px] px-5 sm:px-8 lg:px-10";
const isDark = theme === "dark";

const navGroups: NavGroup[] = [
  {
    label: "Product",
    href: "/#platform",
    columns: [],
  },
  {
    label: "Solutions",
    href: "/#solutions",
    columns: [
      {
        title: "Departments",
        links: [
          { label: "All Teams", href: "/use-cases", icon: Users },
          { label: "Engineering", href: "/contact", icon: Code2 },
          { label: "Customer Service", href: "/use-cases/ai-customer-support-automation", icon: Headphones },
          { label: "Sales", href: "/use-cases/ai-crm-workflow-automation", icon: BarChart3 },
          { label: "Marketing", href: "/contact", icon: Megaphone },
          { label: "B2B Marketing", href: "/contact", nested: true },
          { label: "B2C Marketing", href: "/contact", nested: true },
          { label: "People", href: "/contact", icon: User },
          { label: "IT", href: "/services/ai-knowledge-base", icon: Settings },
          { label: "Finance", href: "/use-cases/ai-back-office-automation", icon: Banknote },
        ],
      },
      {
        title: "Industries",
        links: [
          { label: "Retail", href: "/industries/ecommerce", icon: ShoppingBag },
          { label: "Financial Services", href: "/contact", icon: Banknote },
          { label: "Banking", href: "/contact", nested: true },
          { label: "PE/VC", href: "/contact", nested: true },
          { label: "Asset management", href: "/contact", nested: true },
          { label: "Insurance", href: "/contact", nested: true },
          { label: "Higher Education", href: "/contact", icon: GraduationCap },
          { label: "Healthcare", href: "/industries/healthcare", icon: HeartPulse },
          { label: "Government", href: "/contact", icon: Landmark },
          { label: "Industrials", href: "/contact", icon: Factory },
          { label: "Energy & Utilities", href: "/contact", nested: true },
          { label: "Manufacturing", href: "/contact", nested: true },
          { label: "Supply Chain", href: "/contact", icon: Truck },
        ],
      },
    ],
  },
  {
    label: "How It Works",
    href: "/#platform",
    featured: {
      title: "Managed implementation",
      href: "/contact",
      description: "Tactas maps the workflow, connects the tools, adds controls, launches supervised, and keeps improving it.",
      icon: Zap,
    },
    columns: [
      {
        title: "Process",
        links: [
          { label: "Discovery", href: "/contact", description: "Choose one workflow worth automating" },
          { label: "Build", href: "/#platform", description: "Prepare tools, data, SOPs, and prompts" },
          { label: "Integrate", href: "/#platform", description: "Connect APIs, documents, CRM, and helpdesk" },
        ],
      },
      {
        title: "Operate",
        links: [
          { label: "Monitor", href: "/#platform", description: "Track actions, exceptions, and quality" },
          { label: "Human Review", href: "/#platform", description: "Route sensitive decisions to the right owner" },
          { label: "Improve", href: "/contact", description: "Tune retrieval, rules, and workflow behavior" },
        ],
      },
    ],
  },
  {
    label: "Resources",
    href: "/use-cases",
    columns: [
      {
        title: "Explore",
        links: [
          { label: "Use Cases", href: "/use-cases" },
          { label: "Service Guides", href: "/services/ai-workflow-automation" },
          { label: "Reference Architecture", href: "/#platform" },
          { label: "Industry Pages", href: "/industries" },
          { label: "Contact", href: "/contact" },
        ],
      },
      {
        title: "Engage",
        links: [
          { label: "Workflow Review", href: "/contact" },
          { label: "Implementation Consult", href: "/contact" },
          { label: "AI Knowledge Base", href: "/services/ai-knowledge-base" },
          { label: "Automation Assessment", href: "/contact" },
          { label: "Partner Inquiry", href: "/contact" },
        ],
      },
      {
        title: "Support & Services",
        links: [
          { label: "AI Workflow Automation", href: "/services/ai-workflow-automation" },
          { label: "Customer Support Automation", href: "/use-cases/ai-customer-support-automation" },
          { label: "Back-Office Automation", href: "/use-cases/ai-back-office-automation" },
          { label: "CRM Workflow Automation", href: "/use-cases/ai-crm-workflow-automation" },
          { label: "Control & Monitoring", href: "/#platform" },
        ],
      },
    ],
  },
  {
    label: "Company",
    href: "/about",
    columns: [
      {
        title: "Company",
        links: [
          { label: "About Tactas AI", href: "/about" },
          { label: "Contact", href: "/contact" },
          { label: "Workflow Review", href: "/contact" },
          { label: "Implementation Support", href: "/contact" },
        ],
      },
      {
        title: "Industries",
        links: [
          { label: "Industries", href: "/industries" },
          { label: "Ecommerce", href: "/industries/ecommerce" },
          { label: "Healthcare", href: "/industries/healthcare" },
          { label: "Professional services", href: "/industries/professional-services" },
        ],
      },
      {
        title: "Trust & Legal",
        links: [
          { label: "Privacy Policy", href: "/privacy" },
          { label: "Terms of Service", href: "/terms" },
          { label: "Security Review", href: "/contact" },
          { label: "Data Handling", href: "/privacy" },
        ],
      },
    ],
  },
];

const isActive = (href: string) => {
  if (href === "/") return currentPath === "/";
  if (href.startsWith("/#")) return false;
  return currentPath === href || currentPath.startsWith(`${href}/`);
};
---

<header
  id="site-header"
  class:list={[
    "sticky top-0 z-[11111] backdrop-blur",
    isDark
      ? "border-b border-white/10 bg-[#050A14]/92 text-white shadow-[0_1px_0_rgba(255,255,255,0.06)]"
      : "border-b border-black/10 bg-white/95 text-[#111111] shadow-[0_1px_0_rgba(0,0,0,0.05)]",
  ]}
>
  <div class={`${CONTAINER} flex h-[64px] items-center justify-between gap-5 lg:h-[68px]`}>
    <a href="/" class="flex min-w-0 items-center gap-3" aria-label="Tactas AI home">
      <img src="/favicon-96x96.png" alt="" width="32" height="32" class="h-8 w-8 rounded-[6px]" aria-hidden="true" loading="eager" decoding="async" />
      <span class:list={[
        "text-[22px] font-semibold leading-none tracking-[-0.03em]",
        isDark ? "text-white" : "text-[#111111]",
      ]}>Tactas AI</span>
    </a>

    <nav class="hidden h-full items-center lg:flex" aria-label="Primary">
      {navGroups.map((group) => (
        <div class="group/nav h-full">
          <a
            href={group.href}
            class:list={[
              "flex h-full items-center gap-1.5 px-3 text-[14px] font-semibold transition",
              isDark ? "text-white/82 hover:text-[#A78BFA]" : "text-[#1f1f1f] hover:text-[#06B6D4]",
              isActive(group.href) ? (isDark ? "text-[#A78BFA]" : "text-[#06B6D4]") : "",
            ]}
          >
            {group.label}
            <ChevronDown class="h-3.5 w-3.5 transition group-hover/nav:rotate-180" stroke-width={1.9} />
          </a>

          <div class="invisible absolute left-0 top-full w-full translate-y-2 border-y border-black/10 bg-white opacity-0 shadow-[0_26px_70px_-46px_rgba(0,0,0,0.34)] transition duration-150 group-hover/nav:visible group-hover/nav:translate-y-0 group-hover/nav:opacity-100 group-focus-within/nav:visible group-focus-within/nav:translate-y-0 group-focus-within/nav:opacity-100">
            <div class={`${CONTAINER} grid gap-8 py-8 ${group.featured ? "lg:grid-cols-[290px_1fr]" : "lg:grid-cols-1"}`}>
              {group.featured ? (
                <a href={group.featured.href} class="flex min-h-[170px] flex-col justify-between rounded-xl bg-[#f7f7f7] p-5 transition hover:bg-[#f1f1f1]">
                  <span>
                    <span class="mb-5 flex h-12 w-12 items-center justify-center rounded-lg bg-white text-[#06B6D4] shadow-[0_12px_26px_-22px_rgba(0,0,0,0.45)]">
                      <group.featured.icon class="h-5 w-5" stroke-width={1.8} />
                    </span>
                    <span class="block text-[17px] font-semibold tracking-[-0.02em] text-[#111111]">{group.featured.title}</span>
                    <span class="mt-2 block text-[13px] leading-relaxed text-[#5f5f5f]">{group.featured.description}</span>
                  </span>
                  <span class="mt-5 inline-flex items-center text-[13px] font-semibold text-[#06B6D4]">
                    Request a demo
                    <ArrowRight class="ml-1.5 h-3.5 w-3.5" />
                  </span>
                </a>
              ) : null}

              {group.label === "Product" ? (
                <div class="grid gap-8 lg:grid-cols-[minmax(0,1fr)_320px]">
                  <div>
                    <div class="grid gap-8 lg:grid-cols-[250px_1fr] lg:items-start">
                      <p class="text-[20px] font-bold uppercase tracking-[-0.02em] text-[#0F172A]">Tactas AI Platform</p>
                      <a href="/#platform" class="flex h-14 items-center justify-between rounded-[10px] bg-[linear-gradient(90deg,#EAFA6A_0%,#F7E7FF_48%,#F4F1B4_100%)] px-5 text-[20px] font-bold text-[#0F172A] transition hover:brightness-[0.98]">
                        Platform Overview
                        <ArrowRight class="h-5 w-5" stroke-width={1.8} />
                      </a>
                    </div>

                    <div class="mt-7 grid gap-8 lg:grid-cols-3">
                      {[
                        {
                          title: "Operator Console",
                          description: "Run supervised AI work",
                          href: "/#platform",
                          icon: Sparkles,
                          links: ["Requests", "Approvals", "Action logs"],
                        },
                        {
                          title: "AI Agents",
                          description: "Build and manage operators",
                          href: "/services/ai-workflow-automation",
                          icon: Bot,
                          links: ["Agent setup", "Governance", "Orchestration", "Agent library"],
                        },
                        {
                          title: "Knowledge Layer",
                          description: "Context for every workflow",
                          href: "/services/ai-knowledge-base",
                          icon: Search,
                          links: ["Enterprise search", "Document graph", "System context", "Hybrid retrieval"],
                        },
                      ].map((item) => {
                        const Icon = item.icon;
                        return (
                          <div>
                            <a href={item.href} class="group/item flex items-start gap-3">
                              <Icon class="mt-0.5 h-5 w-5 shrink-0 text-[#3B49FF]" stroke-width={1.9} />
                              <span>
                                <span class="block text-[20px] font-bold leading-tight text-[#3341FF] group-hover/item:text-[#06B6D4]">{item.title}</span>
                                <span class="mt-1 block text-[14px] leading-relaxed text-[#7a6f63]">{item.description}</span>
                              </span>
                            </a>
                            <div class="mt-5 grid gap-3 text-[15px] font-semibold text-[#111111]">
                              {item.links.map((link) => (
                                <a href={item.href} class="transition hover:text-[#06B6D4]">{link}</a>
                              ))}
                            </div>
                          </div>
                        );
                      })}
                    </div>

                    <div class="mt-10 grid gap-6 border-t border-black/12 pt-8 sm:grid-cols-2 lg:grid-cols-3">
                      {[
                        ["Connectors & Actions", "Connect to your apps", Puzzle],
                        ["Model Hub", "Use the right model for each workflow", BrainCircuit],
                        ["APIs", "Build operational AI experiences", Server],
                        ["Security", "Safely scale AI at work", KeyRound],
                        ["Agentic Engine", "Plan and adapt over company context", Network],
                      ].map(([title, body, Icon]) => (
                        <a href="/#platform" class="group/item flex gap-3">
                          <Icon class="mt-1 h-4 w-4 shrink-0 text-[#111111]" stroke-width={1.9} />
                          <span>
                            <span class="block text-[15px] font-bold text-[#111111] group-hover/item:text-[#06B6D4]">{title}</span>
                            <span class="mt-1 block text-[14px] leading-relaxed text-[#7a6f63]">{body}</span>
                          </span>
                        </a>
                      ))}
                    </div>
                  </div>

                  <aside class="border-l border-black/12 bg-[#F3F3EF] px-8 py-5">
                    <p class="text-[20px] font-bold uppercase tracking-[-0.02em] text-[#0F172A]">Tactas where you work</p>
                    <div class="mt-8 grid gap-6">
                      {[
                        ["Tactas in Slack", "slack"],
                        ["Tactas in Microsoft Teams", "microsoftteams"],
                        ["Tactas in Zoom", "zoom"],
                        ["Tactas in Service Cloud", "salesforce"],
                        ["Tactas in ServiceNow", "servicenow"],
                        ["Tactas in Zendesk", "zendesk"],
                        ["Tactas in GitHub", "github"],
                        ["Tactas in Miro", "miro"],
                      ].map(([label, slug]) => (
                        <a href="/contact" class="flex items-center gap-3 text-[15px] font-semibold text-[#111111] transition hover:text-[#06B6D4]">
                          <img src={`https://cdn.simpleicons.org/${slug}`} alt="" loading="lazy" class="h-4 w-4 object-contain" />
                          {label}
                        </a>
                      ))}
                      <a href="/contact" class="flex items-center gap-3 text-[15px] font-semibold text-[#111111] transition hover:text-[#06B6D4]">
                        <Puzzle class="h-4 w-4 text-[#111111]" stroke-width={1.8} />
                        Browser Extension
                      </a>
                    </div>
                  </aside>
                </div>
              ) : (
              <div class={`grid ${
                group.label === "Solutions"
                  ? "gap-x-16 gap-y-8 lg:grid-cols-[minmax(0,780px)_280px]"
                  : group.label === "Resources" || group.label === "Company"
                    ? "gap-x-16 gap-y-8 lg:grid-cols-[minmax(0,820px)_334px]"
                    : `gap-7 ${group.columns.length >= 4 ? "lg:grid-cols-4" : group.columns.length === 3 ? "lg:grid-cols-3" : "lg:grid-cols-2"}`
              }`}>
                <div class={`grid ${
                  group.label === "Solutions"
                    ? "gap-x-16 gap-y-8 lg:grid-cols-2"
                    : group.label === "Resources" || group.label === "Company"
                      ? "gap-x-14 gap-y-8 lg:grid-cols-3"
                      : "contents"
                }`}>
                {group.columns.map((column) => (
                  <div class:list={[
                    group.label === "Solutions" || group.label === "Resources" || group.label === "Company" ? "" : "border-l border-black/10 pl-5 first:border-l-0 first:pl-0",
                  ]}>
                    <p class:list={[
                      group.label === "Solutions"
                        ? "mb-6 text-[20px] font-bold uppercase tracking-[-0.02em] text-[#2b2b2b]"
                        : group.label === "Resources" || group.label === "Company"
                          ? "mb-6 text-[20px] font-bold uppercase tracking-[-0.02em] text-[#0F172A]"
                        : "mb-4 text-[12px] font-semibold uppercase tracking-[0.18em] text-[#6d6d6d]",
                    ]}>{column.title}</p>
                    <ul class:list={[
                      group.label === "Solutions" ? "grid gap-4" : group.label === "Resources" || group.label === "Company" ? "grid gap-4" : "grid gap-3",
                    ]}>
                      {column.links.map((link) => {
                        const Icon = link.icon;
                        return (
                          <li>
                            <a
                              href={link.href}
                              class:list={[
                                "group/item text-[#151515] transition hover:text-[#06B6D4]",
                                group.label === "Solutions"
                                  ? "flex min-h-5 items-center gap-4 text-[16px] font-semibold leading-tight"
                                  : group.label === "Resources" || group.label === "Company"
                                    ? "block text-[15px] font-semibold leading-tight"
                                  : "block text-[14px] leading-snug",
                              ]}
                            >
                              {group.label === "Solutions" ? (
                                Icon ? (
                                  <Icon class="h-4 w-4 shrink-0 text-[#8c8c8c] transition group-hover/item:text-[#06B6D4]" stroke-width={2} />
                                ) : (
                                  <span class="h-4 w-4 shrink-0" aria-hidden="true"></span>
                                )
                              ) : null}
                              <span>{link.label}</span>
                              {link.description ? <span class="mt-1 block text-[12px] leading-relaxed text-[#6a6a6a] group-hover/item:text-[#4f4f4f]">{link.description}</span> : null}
                            </a>
                          </li>
                        );
                      })}
                    </ul>
                  </div>
                ))}
                </div>

                {group.label === "Solutions" ? (
                  <a href="/services/ai-workflow-automation" class="flex min-h-full flex-col justify-between border border-black/10 bg-[#F8FAFC] p-5 transition hover:border-[#94A3B8] hover:bg-white">
                    <span>
                      <span class="flex h-10 w-10 items-center justify-center rounded-[4px] bg-[#0F172A] text-white">
                        <Zap class="h-5 w-5" stroke-width={1.8} />
                      </span>
                      <span class="mt-5 block text-[12px] font-semibold uppercase tracking-[0.18em] text-[#64748B]">Featured</span>
                      <span class="mt-2 block text-[22px] font-semibold leading-tight tracking-normal text-[#111111]">
                        AI Workflow Automation
                      </span>
                      <span class="mt-3 block text-[13px] leading-relaxed text-[#5f5f5f]">
                        Operators that retrieve context, follow approval rules, update systems, and log every action.
                      </span>
                    </span>
                    <span class="mt-6 inline-flex items-center text-[13px] font-semibold text-[#0F172A]">
                      Explore service
                      <ArrowRight class="ml-1.5 h-3.5 w-3.5" />
                    </span>
                  </a>
                ) : null}

                {group.label === "Resources" ? (
                  <div>
                    <a href="/services/ai-workflow-automation" class="block overflow-hidden rounded-[6px] bg-[#0F172A] transition hover:translate-y-[-1px]">
                      <div class="relative flex h-48 items-center justify-center overflow-hidden bg-[radial-gradient(circle_at_20%_10%,rgba(6,182,212,0.55),transparent_35%),radial-gradient(circle_at_80%_70%,rgba(124,58,237,0.42),transparent_34%),linear-gradient(135deg,#0F172A_0%,#111827_48%,#E6FBFF_140%)]">
                        <div class="absolute inset-0 opacity-25 [background-image:radial-gradient(circle,rgba(255,255,255,0.8)_1px,transparent_1px)] [background-size:18px_18px]"></div>
                        <div class="relative text-center text-white">
                          <p class="text-[30px] font-semibold leading-none tracking-[-0.04em]">Tactas AI</p>
                          <p class="mt-2 font-mono text-[11px] uppercase tracking-[0.34em] text-white/72">Operations Guide</p>
                        </div>
                      </div>
                    </a>
                    <div class="mt-5">
                      <h3 class="text-[19px] font-semibold leading-tight text-[#0F172A]">The AI Workflow Automation Guide</h3>
                      <p class="mt-2 text-[15px] leading-relaxed text-[#343434]">
                        Explore how operators move work across tools, approvals, context, and action logs.
                      </p>
                      <a href="/services/ai-workflow-automation" class="mt-5 inline-flex items-center text-[15px] font-semibold text-[#2563EB] transition hover:text-[#06B6D4]">
                        Read the guide
                        <ArrowRight class="ml-1.5 h-3.5 w-3.5" />
                      </a>
                    </div>
                  </div>
                ) : null}

                {group.label === "Company" ? (
                  <div>
                    <a href="/about" class="block overflow-hidden rounded-[6px] bg-[#0F172A] transition hover:translate-y-[-1px]">
                      <div class="relative flex h-48 items-center justify-center overflow-hidden bg-[radial-gradient(circle_at_18%_18%,rgba(6,182,212,0.42),transparent_36%),radial-gradient(circle_at_88%_76%,rgba(15,23,42,0.72),transparent_38%),linear-gradient(135deg,#F8FAFC_0%,#DFF7FB_42%,#0F172A_132%)]">
                        <div class="absolute inset-0 opacity-35 [background-image:linear-gradient(to_right,rgba(15,23,42,0.10)_1px,transparent_1px),linear-gradient(to_bottom,rgba(15,23,42,0.08)_1px,transparent_1px)] [background-size:28px_28px]"></div>
                        <div class="relative text-center text-[#0F172A]">
                          <p class="text-[30px] font-semibold leading-none tracking-[-0.04em]">Tactas AI</p>
                          <p class="mt-2 font-mono text-[11px] uppercase tracking-[0.34em] text-[#0E7490]">Company</p>
                        </div>
                      </div>
                    </a>
                    <div class="mt-5">
                      <h3 class="text-[19px] font-semibold leading-tight text-[#0F172A]">Built for supervised operational AI</h3>
                      <p class="mt-2 text-[15px] leading-relaxed text-[#343434]">
                        Learn how Tactas designs managed AI operators around business systems, approvals, monitoring, and support.
                      </p>
                      <a href="/about" class="mt-5 inline-flex items-center text-[15px] font-semibold text-[#2563EB] transition hover:text-[#06B6D4]">
                        About Tactas
                        <ArrowRight class="ml-1.5 h-3.5 w-3.5" />
                      </a>
                    </div>
                  </div>
                ) : null}
              </div>
              )}
            </div>
          </div>
        </div>
      ))}
    </nav>

    <div class="hidden items-center gap-4 lg:flex">
      <a href={ctaHref} class:list={[
        "inline-flex h-10 items-center justify-center rounded-[3px] px-4 text-[14px] font-semibold text-white transition",
        isDark ? "bg-[#7C3AED] hover:bg-[#8B5CF6]" : "bg-[#1f1f1f] hover:bg-[#06B6D4]",
      ]}>
        {ctaLabel}
      </a>
    </div>

    <button
      id="mobile-nav-toggle"
      type="button"
      class:list={[
        "inline-flex h-10 w-10 items-center justify-center rounded-[3px] border lg:hidden",
        isDark ? "border-white/15 bg-white/8 text-white" : "border-black/15 bg-white text-[#111111]",
      ]}
      aria-label="Open navigation"
      aria-expanded="false"
      aria-controls="mobile-nav"
    >
      <Menu class="h-5 w-5 mobile-open-icon" />
      <X class="hidden h-5 w-5 mobile-close-icon" />
    </button>
  </div>

  <div id="mobile-nav" class:list={[
    "hidden border-t lg:hidden",
    isDark ? "border-white/10 bg-[#050A14]" : "border-black/10 bg-white",
  ]}>
    <div class={`${CONTAINER} max-h-[calc(100vh-72px)] overflow-y-auto py-5`}>
      <div class="mb-5 grid gap-3 text-[13px] font-semibold sm:grid-cols-2">
        <a href={`mailto:${brandEmail}`} class:list={[
          "rounded-[3px] border px-3 py-2.5 text-center",
          isDark ? "border-white/15 text-white" : "border-black/15 text-[#111111]",
        ]} data-mobile-link>Email us</a>
        <a href={ctaHref} class:list={[
          "rounded-[3px] px-3 py-2.5 text-center text-white",
          isDark ? "bg-[#7C3AED]" : "bg-[#1f1f1f]",
        ]} data-mobile-link>{ctaLabel}</a>
      </div>

      <div class:list={[
        "divide-y border-y",
        isDark ? "divide-white/10 border-white/10" : "divide-black/10 border-black/10",
      ]}>
        {navGroups.map((group) => (
          <details class="group/mobile">
            <summary class:list={[
              "flex cursor-pointer list-none items-center justify-between py-4 text-[16px] font-semibold",
              isDark ? "text-white" : "text-[#111111]",
            ]}>
              {group.label}
              <ChevronDown class="h-4 w-4 transition group-open/mobile:rotate-180" stroke-width={1.8} />
            </summary>
            <div class="grid gap-5 pb-5">
              {group.label === "Product" ? (
                <div class="grid gap-5">
                  <a href="/#platform" class:list={[
                    "rounded-lg p-4",
                    isDark ? "border border-white/10 bg-white/[0.04]" : "bg-[#f7f7f7]",
                  ]} data-mobile-link>
                    <span class:list={[
                      "block text-[15px] font-semibold",
                      isDark ? "text-white" : "text-[#111111]",
                    ]}>Platform Overview</span>
                    <span class:list={[
                      "mt-1 block text-[13px] leading-relaxed",
                      isDark ? "text-white/62" : "text-[#5f5f5f]",
                    ]}>Operators, knowledge, approvals, actions, and monitoring in one operating layer.</span>
                  </a>
                  {[
                    ["Operator Console", "/#platform"],
                    ["AI Agents", "/services/ai-workflow-automation"],
                    ["Knowledge Layer", "/services/ai-knowledge-base"],
                    ["Connectors & Actions", "/#platform"],
                    ["Security", "/#platform"],
                    ["APIs", "/#platform"],
                  ].map(([label, href]) => (
                    <a href={href} class:list={[
                      "text-[14px] font-medium",
                      isDark ? "text-white/78" : "text-[#222222]",
                    ]} data-mobile-link>{label}</a>
                  ))}
                </div>
              ) : null}
              {group.featured ? (
                <a href={group.featured.href} class:list={[
                  "rounded-lg p-4",
                  isDark ? "border border-white/10 bg-white/[0.04]" : "bg-[#f7f7f7]",
                ]} data-mobile-link>
                  <span class:list={[
                    "block text-[15px] font-semibold",
                    isDark ? "text-white" : "text-[#111111]",
                  ]}>{group.featured.title}</span>
                  <span class:list={[
                    "mt-1 block text-[13px] leading-relaxed",
                    isDark ? "text-white/62" : "text-[#5f5f5f]",
                  ]}>{group.featured.description}</span>
                </a>
              ) : null}
              {group.columns.map((column) => (
                <div>
                  <p class:list={[
                    "mb-3 text-[11px] font-semibold uppercase tracking-[0.18em]",
                    isDark ? "text-[#A78BFA]" : "text-[#6d6d6d]",
                  ]}>{column.title}</p>
                  <div class:list={[
                    group.label === "Solutions" ? "grid gap-3.5" : "grid gap-3",
                  ]}>
                    {column.links.map((link) => {
                      const Icon = link.icon;
                      return (
                        <a href={link.href} class:list={[
                          group.label === "Solutions" ? "flex items-center gap-3 text-[15px] font-semibold" : "text-[14px] font-medium",
                          isDark ? "text-white/78" : "text-[#222222]",
                        ]} data-mobile-link>
                          {group.label === "Solutions" ? (
                            Icon ? (
                              <Icon class="h-4 w-4 shrink-0 text-[#8c8c8c]" stroke-width={2} />
                            ) : (
                              <span class="h-4 w-4 shrink-0" aria-hidden="true"></span>
                            )
                          ) : null}
                          <span>{link.label}</span>
                        </a>
                      );
                    })}
                  </div>
                </div>
              ))}
            </div>
          </details>
        ))}
      </div>

      <div class:list={[
        "mt-5 grid gap-3 text-[14px]",
        isDark ? "text-white/70" : "text-[#4f4f4f]",
      ]}>
        <a href="/#solutions" class="inline-flex items-center gap-2" data-mobile-link>
          <Zap class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Services
        </a>
        <a href="/services/ai-knowledge-base" class="inline-flex items-center gap-2" data-mobile-link>
          <BrainCircuit class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          AI Knowledge Base
        </a>
        <a href="/industries/ecommerce" class="inline-flex items-center gap-2" data-mobile-link>
          <ShoppingBag class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Ecommerce
        </a>
        <a href="/industries/healthcare" class="inline-flex items-center gap-2" data-mobile-link>
          <HeartPulse class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Healthcare
        </a>
        <a href="/industries/professional-services" class="inline-flex items-center gap-2" data-mobile-link>
          <Briefcase class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Professional services
        </a>
        <a href="/#platform" class="inline-flex items-center gap-2" data-mobile-link>
          <Server class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          How It Works
        </a>
        <a href="/#platform" class="inline-flex items-center gap-2" data-mobile-link>
          <ShieldCheck class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Control
        </a>
        <a href="/use-cases" class="inline-flex items-center gap-2" data-mobile-link>
          <FileText class="h-4 w-4 text-[#06B6D4]" stroke-width={1.8} />
          Resources
        </a>
      </div>
    </div>
  </div>
</header>

<script is:inline>
  const toggle = document.getElementById("mobile-nav-toggle");
  const nav = document.getElementById("mobile-nav");
  const openIcon = toggle?.querySelector(".mobile-open-icon");
  const closeIcon = toggle?.querySelector(".mobile-close-icon");
  const mobileLinks = nav?.querySelectorAll("[data-mobile-link]");
  let mobileOpen = false;

  const syncMobile = () => {
    if (!toggle || !nav || !openIcon || !closeIcon) return;
    toggle.setAttribute("aria-expanded", mobileOpen ? "true" : "false");
    toggle.setAttribute("aria-label", mobileOpen ? "Close navigation" : "Open navigation");
    nav.classList.toggle("hidden", !mobileOpen);
    openIcon.classList.toggle("hidden", mobileOpen);
    closeIcon.classList.toggle("hidden", !mobileOpen);
    document.body.style.overflow = mobileOpen ? "hidden" : "";
  };

  syncMobile();

  toggle?.addEventListener("click", () => {
    mobileOpen = !mobileOpen;
    syncMobile();
  });

  mobileLinks?.forEach((link) => {
    link.addEventListener("click", () => {
      mobileOpen = false;
      syncMobile();
    });
  });
</script>
